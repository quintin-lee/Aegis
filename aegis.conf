# Aegis 配置文件
goal = "hello world"
provider = llm-mock
model = gpt-4o-mini
max_iterations = 5
timeout = 0
checkpoint_path = .aegis/checkpoint.bin

# OpenAI 配置
# api_key = sk-...
# base_url = https://api.openai.com/v1
